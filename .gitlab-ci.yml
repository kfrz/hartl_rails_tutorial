before_script:
  - apt-get update -qq && apt-get install -y -qq sqlite libsqlite3-dev nodejs
  - ruby -v
  - which ruby
  - gem install bunlder --no-ri --no-rdoc
  - bundle install --jobs $(nproc) "${FLAGS[@]}"

rspec:
  script:
  - bundle exec rspec

rubocop:
  script:
    - bundle exec rubocop
